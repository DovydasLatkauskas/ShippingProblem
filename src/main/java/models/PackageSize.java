package models;

/**
 * Represents different delivery package sizes: S, M, and L.
 */
public enum PackageSize {
    // in order to add more package sizes, you have to add their delivery prices in DeliveryMethod
    S,
    M,
    L
}
